```bash
cd my_poetry


# 1. Initialize Poetry (creates pyproject.toml)
poetry init

# 2. Add dependencies
poetry add requests beautifulsoup4
poetry add pandas 

# 3.run commands
poetry run python my_script.py

# 4. Activate a shell within the project's environment
poetry shell

# 5. install projects dependencies
poetry install

```

